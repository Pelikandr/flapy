using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test_settings : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Debug.Log(Global.music);
        Debug.Log(Global.sound);
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
